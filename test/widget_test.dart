// Widget smoke tests are intentionally minimal. Full widget tests require
// Firebase initialisation and DI setup, which is better suited for
// integration tests or test harnesses with mocked Firebase.
//
// The actual logic is covered by unit tests in the feature-specific
// test/ sub-directories.

import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder — replace with real widget smoke tests once Firebase mocking is set up', () {
    expect(true, isTrue);
  });
}
